export const EMAIL_PATTERN = /[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,3}$/;
export const PHONE_NUM_PATTERN =
  /^\+7(\s+)?[0-9]{3}?(\s+)?[0-9]{3}?[0-9]{2}?[0-9]{2}$/;
// TODO: check убрать при деплое на реальный серв
